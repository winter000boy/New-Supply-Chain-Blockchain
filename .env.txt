# Blockchain Configuration
GANACHE_PORT=7545
GANACHE_HOST=127.0.0.1
NETWORK_ID=5777

# Truffle Configuration
TRUFFLE_MNEMONIC="your twelve word mnemonic phrase here"
TRUFFLE_PRIVATE_KEY="your-private-key-here"

# Infura Configuration (for deploying to testnets or mainnet)
INFURA_PROJECT_ID=your-infura-project-id
INFURA_PROJECT_SECRET=your-infura-project-secret
INFURA_RINKEBY_URL=https://rinkeby.infura.io/v3/your-infura-project-id
INFURA_GOERLI_URL=https://goerli.infura.io/v3/your-infura-project-id

# MetaMask Configuration
METAMASK_PRIVATE_KEY="your-metamask-private-key"

# Server Configuration
SERVER_PORT=5000
SERVER_HOST=127.0.0.1

# Database Configuration (if applicable)
DB_HOST=127.0.0.1
DB_PORT=27017
DB_NAME=supply_chain_db
DB_USER=your-db-username
DB_PASSWORD=your-db-password

# JWT Configuration (if using authentication)
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=1d

# Other Configuration
NODE_ENV=development